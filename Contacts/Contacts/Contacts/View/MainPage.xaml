<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Contacts"
             x:Class="Contacts.MainPage"
             x:Name="ContactsList">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="ToolbarItem_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <SearchBar Placeholder="Pesquisar contactos" TextChanged="SearchBar_TextChanged"/>
            <ListView ItemsSource="{Binding People}" ItemSelected="ListView_ItemSelected" HasUnevenRows="True" x:Name="PeopleListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ContentView>
                                <StackLayout Orientation="Horizontal">
                                    <Image Source="{Binding ImageSource}" WidthRequest="75" HeightRequest="75" HorizontalOptions="Start"></Image>
                                    <Label Text="{Binding Name}" FontSize="20" FontAttributes="Bold"></Label>
                                    <Label Text="{Binding Address}" FontSize="20" FontAttributes="Bold"></Label>
                                    <Button Text="Ligar" HorizontalOptions="End" CommandParameter="{Binding .}" Clicked="Button_Clicked" Command="{Binding Path=BindingContext.startCallCommand, Source={x:Reference ContactsList}}"></Button>
                                </StackLayout>
                            </ContentView>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>